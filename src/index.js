import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


// react dom is rendering the "app" component into the id of root
ReactDOM.render(<App />, document.getElementById('root'));
